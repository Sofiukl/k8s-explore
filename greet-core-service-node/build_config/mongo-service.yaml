apiVersion: v1
kind: Service
metadata:
    name: mongo-service
    namespace: default
    labels:
        name: mongo-service
        app: greet-app
spec:
    type: ClusterIP
    selector:
        name: mongo-pod
        app: greet-app
    ports:
        - port: 27017
    