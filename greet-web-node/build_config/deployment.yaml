apiVersion: apps/v1
kind: Deployment
metadata:
  name: greeter-web
  namespace: default
  labels:
    name: greeter-web
    app: greet-app
spec:
  replicas: 1
  selector:
    matchLabels:
      name: greeter-web-pod
      app: greet-app
  template:
    metadata:
      name: greeter-web-pod
      namespace: default
      labels:
        name: greeter-web-pod
        app: greet-app
    spec:
      containers:
        - name: greeter-web-container
          image: gcr.io/k8s-explore-309407/greeter-web:v1
          ports:
            - containerPort: 8000
          env:
            - name: GREET_EXT_SERVICE
              value: http://34.123.254.83:3001/
